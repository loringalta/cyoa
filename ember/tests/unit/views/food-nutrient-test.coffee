`import { moduleFor, test } from 'ember-qunit'`

moduleFor 'view:food-nutrient', 'Unit | View | food nutrient'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
