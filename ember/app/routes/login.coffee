`import Ember from 'ember'`

Login = Ember.Route.extend()
  # actions:
  # 	authenticate: ->
  # 		credentials = @controller.getProperties 'identification', 'password'
  # 		authenticator = 'simple-auth-authenticator:devise'
  # 		@get('session').authenticate(authenticator, credentials).then ((data) -> 
  # 			console.log data
  # 		)

`export default Login`