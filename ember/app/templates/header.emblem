nav
  ul
    li#login
      if session.isAuthenticated
        a{ action 'invalidateSession' } Logout
      else
        link-to "login" | Login
    li#home
      link-to "index" | Home
    li#search
      link-to "search-bar" | Search
